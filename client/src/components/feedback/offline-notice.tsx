export function OfflineNotice() {
  return (
    <div className="alert alert-warning mb-0 rounded-0 text-center" role="status">
      You are offline. Changes will sync when the connection returns.
    </div>
  );
}
